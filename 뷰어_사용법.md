# 리포트 뷰어 사용법

`viewer.html` — 종목을 선택하면 해당 리포트가 바로 뜨는 뷰어.
지금 GitHub에서 HTML을 클릭하면 코드(raw)만 보이는 이유는, GitHub이
레포 안의 .html을 **렌더링하지 않고 소스로** 보여주기 때문입니다.
해결은 **GitHub Pages**를 켜서 실제 웹페이지로 서비스하는 것입니다.

## 설치 (한 번만)

1. `viewer.html` 을 레포 **루트**에 커밋 (data/ 폴더와 같은 위치).
2. 레포 → **Settings → Pages**
   - Source: **Deploy from a branch**
   - Branch: `main` / 폴더 `/ (root)` → Save
3. 1~2분 뒤 주소가 발급됩니다:
   `https://<사용자명>.github.io/<레포명>/viewer.html`
   이 주소를 폰 홈화면에 추가하면 앱처럼 씁니다.

## 작동 방식

- 매 실행마다 `data/latest/manifest.json` 이 자동 생성됩니다
  (종목 목록 + 등락률 + 지수/보유종목 구분).
- 뷰어는 이 manifest 를 읽어 사이드바/드롭다운을 구성하고,
  종목을 고르면 `data/latest/<코드>_<이름>.html` 을 iframe 으로 띄웁니다.
- 항상 **최신본(latest)** 을 보여줍니다. `↻ 새로고침` 으로 다시 로드.

## 리포트가 "코드로만" 보이던 문제

- GitHub 저장소에서 직접 .html 링크를 누르면 소스가 보입니다(정상).
- **viewer.html 을 Pages 주소로 열면** 제대로 렌더링된 페이지로 보입니다.
- 즉, 앞으로는 GitHub 파일 목록이 아니라 **Pages 의 viewer.html 주소**로 접속하세요.

## 비공개 레포인 경우

- GitHub Pages 는 무료 플랜에서 **공개 레포**만 됩니다.
- 레포를 비공개로 두려면 holdings 등 민감정보가 없는지 확인하거나,
  (a) 별도 공개 레포에 data/ 만 push 하거나
  (b) Netlify/Cloudflare Pages(비공개 레포도 무료 배포 가능)를 쓰면 됩니다.

## 로컬에서 바로 보기 (Pages 없이)

레포를 clone 한 폴더에서:
```bash
python -m http.server 8000
# 브라우저에서 http://localhost:8000/viewer.html
```
(그냥 viewer.html 을 더블클릭하면 브라우저 보안정책으로 manifest fetch 가
막힐 수 있어, 위처럼 로컬 서버로 여는 게 확실합니다.)
